% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modeling.R
\name{train_rf_model}
\alias{train_rf_model}
\title{Train a Random Forest model}
\usage{
train_rf_model(d, formula, ntree = 100, mtry = 2, seed = 123)
}
\arguments{
\item{d}{A data frame containing the training data}

\item{formula}{Model formula to use for training (required)}

\item{ntree}{Number of trees to grow. Default: 100}

\item{mtry}{Number of variables randomly sampled at each split. Default: 2}

\item{seed}{Random seed for reproducibility. Default: 123}
}
\value{
A trained randomForest model object
}
\description{
Trains a Random Forest model using the provided formula and data. Handles both standard
and dot notation in formula. Checks for missing variables and ensures reproducibility via seed.
}
\examples{
\dontrun{
# Example data
df <- data.frame(
  adopted = factor(c("Yes", "No", "Yes", "No")),
  age = c(2, 5, 1, 4),
  sex = factor(c("M", "F", "F", "M"))
)

# Train model
model <- train_rf_model(df, adopted ~ age + sex)
}

}

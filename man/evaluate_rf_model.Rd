% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modeling.R
\name{evaluate_rf_model}
\alias{evaluate_rf_model}
\title{Evaluate a classification model}
\usage{
evaluate_rf_model(
  model,
  test_d,
  target_col = "adopted",
  positive_level = "Yes",
  negative_level = "No"
)
}
\arguments{
\item{model}{Trained model object (e.g., randomForest)}

\item{test_d}{A data frame containing the test data}

\item{target_col}{The name of the target column. Default: "adopted"}

\item{positive_level}{The positive class level. Default: "Yes"}

\item{negative_level}{The negative class level. Default: "No"}
}
\value{
A list containing: confusion_matrix (the caret confusionMatrix object),
metrics (dataframe with performance metrics), and cm_summary (confusion matrix summary)
}
\description{
This function evaluates a trained model on test data, computing confusion
matrix, metrics, and summary statistics for binary classification.
}
\examples{
\dontrun{
# Example dummy data for testing
test_data <- data.frame(adopted = factor(c("Yes", "No")),
                        feature1 = c(1.2, 3.4),
                        feature2 = c("A", "B"))
rf_model <- randomForest::randomForest(adopted ~ ., data = test_data)
results <- evaluate_rf_model(rf_model, test_data)

# Access components
metrics_df <- results$metrics
conf_matrix <- results$confusion_matrix
}
}
